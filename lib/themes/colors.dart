import 'package:flutter/material.dart';

class AppColors {
  // Light mode primary color
  static const Color primaryLight = Color(0xFF002244);
  // Dark mode primary color
  static const Color primaryDark = Color(0xFF1E1E2C);

  // Light mode background
  static const Color backgroundLight = Color(0xFFF1F1F1);
  // Dark mode background
  static const Color backgroundDark = Color(0xFF121212);

  // Text color for Light mode
  static const Color textPrimaryLight = Colors.black;
  // Text color for Dark mode
  static const Color textPrimaryDark = Colors.white;

  // Secondary text colors for Light and Dark modes
  static const Color textSecondaryLight = Colors.black54;
  static const Color textSecondaryDark = Colors.white70;

  // Accent color for buttons and highlights
  static const Color accent = Colors.deepPurple;
}
